language: ruby
services:
- postgresql
rvm:
- 2.5.0

install:
- gem install bundler
- bundle install
- bundle exec rake db:create
- bundle exec rake db:migrate

script: 
- bundle exec rspec
- bundle exec rubocop